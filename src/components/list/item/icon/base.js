import {
    ListItemIcon,
    withStyles
} from '@material-ui/core';

const styles = {
    root: {
        color: `#5BD6F7`
    }
};

const ListItemIconBase = withStyles(styles)(ListItemIcon);

export default ListItemIconBase;
