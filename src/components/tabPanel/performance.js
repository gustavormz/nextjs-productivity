const TabPanePerformance = props => (
    <div
        style={{
            width: `100%`,
            padding: 10
        }}
        {...props}>
        <p>Performance view</p>
    </div>
);

export default TabPanePerformance;
